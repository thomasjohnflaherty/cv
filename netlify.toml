[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Deploy all branches — each gets its own preview URL
# e.g. v2--thomflaherty.netlify.app
[context.branch-deploy]
  command = "npm run build"
  publish = "dist"
